#include "main.h"

/**
 * _puts - prints a string followed by a new line
 * @str: input str
 * Return: no return
 */
void _puts(char *s)
{
	int i = 0;

	while (i >= 0)
	{
		if (s[i] == '\0')
		{
			break;
		}
		i++;
	}
}
